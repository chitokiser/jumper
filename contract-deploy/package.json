{
  "name": "jumper-contract-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx hardhat run scripts/deploy.js --network opbnb"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
