# 가맹점 연동 API 주의사항

1. 가맹점 제공용 홈페이지/API 연동 시 **절대 K-MOA (케이모아) 브랜드명이나 관련 용어를 노출해서는 안 됩니다**. 
   - 가맹점 입장에서는 본인들의 독자적인 플랫폼으로 보여야 하므로 (화이트라벨링), API 반환값 내부의 주석이나 에러 메시지 역시 "Platform"과 같은 중립적인 단어를 사용해야 합니다.
   - 이미 항의가 접수된 바 있으니 향후 백엔드/프론트엔드 업데이트 시 철저히 체크해야 합니다.

2. 가맹점주 잔액(KM, Point, BT) 동기화
   - 가맹점주의 자산 로직은 **일반 회원(users)**과 100% 동일한 테이블/로직을 공유합니다.
   - 외부 가맹점 API(`merchantApi.js`) 역시 `users` 컬렉션의 `ownerUid` 데이터를 기반으로 조회하여 마이페이지와 완벽히 동일한 잔액을 가져가도록 구현되어 있습니다.
