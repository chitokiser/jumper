# 스마트 멤버십 플랫폼 API 연동 작업 지시서

**수신**: 제휴 가맹점 프론트엔드/웹 개발 담당자
**목적**: 화이트라벨 멤버십 및 포인트/재화(BT, KM, Point) 기능 홈페이지 연동
**적용 일자**: 즉시

---

## 1. 연동 기본 정책 (필독)
-본 API는 가맹점의 독자적인 멤버십 생태계를 구축하기 위해 제공되는 **화이트라벨(White-label) 백엔드 엔진**입니다.
- **[주의 사항]** 가맹점 홈페이지 및 앱 화면 개발 시 'K-MOA' 또는 타사의 브랜드명을 암시하는 문구나 로고를 **절대 노출하지 마십시오.** 오직 귀사(예: 대한김치)의 고유 포인트/마일리지 시스템인 것처럼 UI/UX를 설계해야 합니다.
- 모든 재화(포인트, KM, BT) 데이터는 내부 중앙 서버의 마이페이지 데이터와 **완벽하게 100% 일치**하는 최신 데이터를 반환하도록 통합되었습니다.

## 2. 권한 및 인증 (Authentication)
* HTTP 요청 시 헤더에 귀사에 할당된 전용 API 키를 반드시 포함해야 합니다.
* **Header Key**: `x-api-key`
* **Header Value**: `moa-merch-XXXXXXXXXXXX` *(새로 발급받으신 고유 문자열 사용)*

## 3. 주요 API 엔드포인트 명세

### A. 가맹점 잔액 및 상태 조회
귀사의 계정에 할당된 멤버십 재화 (보유 포인트, 결제대금, BT) 잔고를 불러옵니다.
- **Method**: `GET`
- **Path**: `(협의된 Cloud Functions URL)/merchantApi/v1/balance`
- **Response**:
```json
{
  "success": true,
  "merchantId": "2",
  "merchantName": "대한김치",
  "balance": {
    "points": 726212,        // 리워드 포인트
    "km": 367620,            // 결제 대금 (VND 베이스)
    "btBalance": 193         // 잔여 보너스 티켓 (BT)
  }
}
```

### B. 가맹점 소속 유저 목록 및 정보 리스트
귀사를 통해 가입했거나 관리 중인 멤버 목록과 그들의 실시간 포인트를 출력합니다.
- **Method**: `GET`
- **Path**: `(협의된 Cloud Functions URL)/merchantApi/v1/members?limit=50`
- **Response**:
```json
{
  "success": true,
  "count": 1,
  "members": [
    {
      "uid": "...",
      "email": "kfu134252@gmail.com",
      "displayName": "홍길동",
      "walletAddress": "0x...",
      "joinedAt": "2026-09-06T00:00:00.000Z",
      "points": 1000,
      "btBalance": 5
    }
  ]
}
```

## 4. 프론트엔드 작업 지시 사항
1. **마이페이지 동일 렌더링**: 위 `/v1/balance` API를 호출하면 중앙 앱의 [마이페이지]와 단위, 금액이 **1원/1BT 단위까지 똑같이 매칭**됩니다. 가맹점 홈페이지 내 프로필 화면에 API 응답값을 그대로 노출시켜 주시기 바랍니다.
2. **에러 핸들링**: API 장애 시 `success: false` 및 `message`가 반환됩니다. 해당 메시지 내용 역시 특정 플랫폼을 언급하지 않는 중립적 단어로 구성되어 있으니, 응답되는 메시지를 고객에게 알럿(Alert)으로 그대로 띄우셔도 무방합니다.
3. **토큰 보안성**: 서버나 프론트에서 `x-api-key` 값이 Git 등의 퍼블릭 저장소에 하드코딩 노출되지 않도록 환경 변수(.env) 기반으로 은닉해주시길 바랍니다.
